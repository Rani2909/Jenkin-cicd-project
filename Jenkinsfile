pipeline {
    agent any

    stages {

        stage('Test') {
            steps {
                sh 'echo Jenkins Working'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}